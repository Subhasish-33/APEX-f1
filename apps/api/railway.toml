[build]
builder = "NIXPACKS"

[deploy]
# Using direct $PORT variable which Railway guarantees is present.
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10